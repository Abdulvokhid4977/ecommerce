import 'package:flutter/cupertino.dart';

class ErrorPage extends StatelessWidget {
  const ErrorPage({super.key, required RouteSettings settings});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
